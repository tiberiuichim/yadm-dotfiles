print("Applying Lazyvim patches...")

-- local function bufferline_numbers(opts)
--   return string.format("%s|%s", opts.id, opts.raise(opts.ordinal))
-- end
--
-- local function patch_bufferline()
--   local uiconfig = require("lazyvim")
--   -- local uiconfig = require("lazyvim.plugins.ui")
--   -- local bufconfig = uiconfig[3]
--   -- print(vim.inspect(bufconfig))
--   -- bufconfig.opts[1].numbers = bufferline_numbers
-- end
--
-- patch_bufferline()

return {}
